// RuntimeAdapter — re-exported from @honeyai/core.
// Adapter implementations: ClaudeCodeAdapter (@honeyai/adapter-claude).
// Tests use mock objects implementing RuntimeAdapter directly.

export type {
  RuntimeAdapter,
  ExecuteNodeParams,
  StreamingNodeEvent,
  ExecutionKind,
} from '@honeyai/core'
